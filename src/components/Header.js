import Button from "./Button";
const Header =()=>{
    return(
        <header className="header">
            <h2>Task Tracker</h2>
            <Button text="Add"/>

        </header>

    );
}
export default Header;